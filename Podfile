# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'VideosMergerDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for VideosMergerDemo
  pod 'CTAssetsPickerController', '~> 3.3.0'
  pod 'Toast', '~> 4.0.0'

  target 'VideosMergerDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'VideosMergerDemoUITests' do
    # Pods for testing
  end

end
